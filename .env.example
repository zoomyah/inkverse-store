# ===== INKVERSE environment =====
# Stripe publishable key (test mode). Leave blank to use the mock checkout.
VITE_STRIPE_PUBLISHABLE_KEY=

# Base URL of a real Django REST backend. Leave blank to use the in-memory mock API.
VITE_API_BASE=
